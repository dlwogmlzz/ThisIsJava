package ch06.sec13.exam02.package1;

public class B {
    // 필드 선언(자신의 클래스 타입으로)
    A a1 = new A(true);
    A a2 = new A(1);
//    A a3 = new A("문자열");    // private 접근제어자를 쓰고 있기때문에 다른 클래스에서는 사용할 수 없다.
}
